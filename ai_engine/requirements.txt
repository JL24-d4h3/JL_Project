# ============================================================
# AI Engine — requirements.txt
# GTR-PUCP CDN Educativa Offline
#
# IMPORTANTE: PyTorch NO se instala desde aquí.
# JetPack 6.x incluye el wheel ARM64+CUDA oficial de NVIDIA.
# Verificar antes: python3 -c "import torch; print(torch.cuda.is_available())"
#
# TensorRT-LLM se instala desde el wheel ARM64 de NVIDIA o compilado
# desde fuente (ver guia-inicio.md §5.2).
# ============================================================

# API
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.0
python-multipart==0.0.9
python-dotenv==1.0.0

# Retrieval vectorial
chromadb==0.5.0
sentence-transformers==3.0.0
rank_bm25==0.2.2

# NLP
spacy==3.7.4
# Instalar modelo español por separado:
#   python3 -m spacy download es_core_news_sm
langdetect==1.0.9

# Transformers (re-ranker, tokenizadores)
transformers==4.41.0
# torch: NO incluir aquí — usar el preinstalado por JetPack

# STT
# openai-whisper tiene setup.py roto en Python 3.12 — usar faster-whisper
faster-whisper

# Ingesta de documentos
pymupdf==1.24.0
pytesseract==0.3.10
python-docx==1.1.2

# Infraestructura
prometheus-client==0.20.0
aiofiles==23.2.0
httpx==0.27.0
diskcache==5.6.3

# TensorRT-LLM: instalar por separado (wheel ARM64 específico)
# pip install --extra-index-url https://pypi.ngc.nvidia.com tensorrt-llm==0.10.0
